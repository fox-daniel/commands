Commands and notes

Shell:

q - quit in shell

control-c - shut down jupyter notebook


ipython:

quit - to close ipython


Home-brew:

brew help —  for help


Questions:

¿in my ipython i don’t get all the same documentation of functions (len) as showing in VanderPlas

¿similarly, attributes of the list L=[1,2,3] don’t include “copy” and “clear” on my machine

range(1,N) is from 1 to N-1

Get the loose standing reference guide for numpy, available from
`the NumPy homepage <https://www.scipy.org>`_.

¿“dense array”

¿float32

¿double-precision float

¿endian

¿p43 my command of “print("itemsize:" , x3.itemsize, "bytes")” produces ('itemsize:', 8, 'bytes') instead of the book’s “itemsize: 8 bytes” 

¿%cpaste - i don’t see how to use this. how does one navigate within the pasted code to edit it?

¿grep and fields - when I try to find them with help or ? or ??, they don’t seem to exist
